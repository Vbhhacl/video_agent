# ── AI Meeting Assistant ── requirements.txt ──────────────────────────────────
# Compatible with Python 3.10/3.11 for Streamlit Cloud deployment

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2024.4.9                  # Download audio from YouTube URLs
pydub>=0.25.1,<0.26.0             # Audio file manipulation / format conversion
ffmpeg-python>=0.2.0              # FFmpeg bindings (FFmpeg binary must be installed separately)

# ── Speech-to-Text (local Whisper) ────────────────────────────────────────────
openai-whisper==20231117          # Stable Whisper build compatible with older Python versions
torch==2.2.2                      # PyTorch backend for Whisper
# torchaudio is not required for the current app flow and can be removed for lighter deployment

# ── Translation (Hindi → English) ─────────────────────────────────────────────
deep-translator>=1.11.4,<2.0.0    # Lightweight translation wrapper (Google backend)

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain>=0.2.0,<1.0.0
langchain-core>=0.2.0,<1.0.0
langchain-community>=0.2.0,<1.0.0
langchain-chroma>=0.0.2,<1.0.0
langchain-mistralai>=0.1.0,<1.0.0
mistralai>=0.4.0,<2.0.0          # Official Mistral Python client

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
chromadb>=0.5.0,<1.0.0            # Local vector store
sentence-transformers>=3.0.0,<4.0.0
langchain-huggingface>=0.0.3,<1.0.0
huggingface-hub>=0.23.0,<1.0.0
tiktoken>=0.7.0,<1.0.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0,<2.0.0
streamlit-extras>=0.4.0,<1.0.0
watchdog>=4.0.0,<5.0.0

# ── PDF & TXT Export ──────────────────────────────────────────────────────────
reportlab>=4.2.0,<6.0.0
fpdf2>=2.7.9,<3.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0
numpy>=1.26.0,<2.0.0
tqdm>=4.66.0,<5.0.0
requests>=2.32.0,<3.0.0